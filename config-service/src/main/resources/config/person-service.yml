server:
  port: 8081

spring:
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: admin

eureka:
 client:
  service-url:
    defaultZone: http://localhost:8761/eureka
